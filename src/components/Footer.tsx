import React from 'react'
import styles from '../app-assets/Footer.module.sass';

const Footer = () => (<footer className={styles['App-footer']}>
    <nav>This is where a potential footer would go</nav>
</footer>)

export default Footer
