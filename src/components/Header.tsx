import React from 'react'
import styles from '../app-assets/Header.module.sass';

const Header = () => (<header className={styles['App-header']}>
    <nav>This is where a potential nav would go</nav>
</header>)

export default Header
